<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mindwiki.dao.NodeDao">

	<select id="existByMindID" parameterType="com.mindwiki.model.NodeDto" resultType="int">
		SELECT EXISTS (SELECT * FROM mind WHERE MindID=#{MindID})
	</select>
	
<!-- 	<insert id="setNode" parameterType="object" > -->
	<insert id="setNode" parameterType="object" >
	
		INSERT INTO
			node  (nodeObject)
			values(#{nodeObject})
		WHERE MindID=#{MindID};
	</insert>
	
	<insert id="initNode" parameterType="object" >
	
		INSERT INTO
			node  (MindID, nodeObject)
			values(#{MindID}, #{nodeObject})
	</insert>
	
<!-- 		insert into node(MindID, nodeJson) values(#{MindID},json_object( -->
<!-- 	    'age', 29, -->
<!-- 	    'gender', 'woman', -->
<!-- 	    '부서', '개발', -->
<!-- 	    '자격증', json_array('CISA', 'PMP', 'CISSP') -->
<!-- 	    )); -->
<!-- 		INSERT INTO node (MindID) VALUES (#{MindID}); -->
		
<!-- 		 ON DUPLICATE KEY UPDATE MindID=#{MindID}; -->
		
<!-- 		INSERT INTO node nodeJson=json_array(#{nodeJson}); -->
<!-- 		SET @json = JSON_ARRAY(#{nodeJson}); -->

<!-- 		INSERT INTO node  -->
<!-- 		(MindId, nodeJson) -->
<!-- 		values -->
<!-- 		(#{MindID}, @json); -->
<!-- 		(#{MindID}, JSON_SET(#{nodeJson})) -->
		
		
<!-- 		) -->
<!-- 		nodeJson=#{nodeJson}; -->
<!-- 		 INSERT INTO node (MindID, nodeJson) -->
<!--                     VALUES (#{MindID}, json_array(#{nodeJson})) -->
<!--                     ON DUPLICATE KEY UPDATE MindID=#{MindID}, nodeJson=json_array(#{nodeJson)}; -->
	
	<select id="getNode" parameterType="com.mindwiki.model.NodeDto" resultType="String">
		SELECT nodeObject FROM node WHERE MindID=#{MindID}
	</select>
		
</mapper>