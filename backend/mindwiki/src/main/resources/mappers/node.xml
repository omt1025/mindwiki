<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mindwiki.dao.NodeDao">

	<select id="existByMindID" parameterType="com.mindwiki.model.NodeDto" resultType="int">
		SELECT EXISTS (SELECT * FROM mind WHERE MindID=#{MindID})
	</select>
	
	<insert id="setNode" parameterType="com.mindwiki.model.NodeDto">
		 INSERT INTO node (MindID, nodeJson)
                    VALUES (#{MindID}, json_array(#{nodeJson}))
                    ON DUPLICATE KEY UPDATE MindID=#{MindID}, nodeJson=json_array(#{nodeJson)};
	</insert>
	
<!-- 	<select id="getNode" parameterType="com.mindwiki.model.NodeDto" resultType="map"> -->
<!-- 		SELECT nodeJson FROM node WHERE MindID=#{MindID} -->
<!-- 	</select> -->
		
</mapper>